name: Build flipChartTests

on:
  workflow_dispatch:
  push:

jobs:
  build:
    name: Build flipChartTests
    permissions:
      checks: write
      contents: read
    uses: Displayr/nixr-public/.github/workflows/build-flake-installable.yaml@master
    with:
      installable-path: pkgs.x86_64-linux.rPackages.flipChartTests
      build-args: --override-input flipChartTests .
      installable-name: flipChartTests
    secrets: inherit
